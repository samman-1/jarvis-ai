# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vercel CLI local project link (contains project/org IDs — keep out of the repo)
.vercel

# Portal login gateway — separate app, becomes its own repo (not part of the marketing site)
portal-gateway
# MEC portal — cloned separately for the SSO integration; its own repo
mec-portal
IMG_1959.MP4

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
